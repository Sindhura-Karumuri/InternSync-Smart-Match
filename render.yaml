services:
  - type: web
    name: pm-internship-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: pm-internship-db
          property: connectionString
      - key: ENVIRONMENT
        value: production

databases:
  - name: pm-internship-db
    databaseName: pm_internship
    user: postgres
